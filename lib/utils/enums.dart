enum BTServiceState {
  waiting,
  discovering,
  done,
  failed,
}

enum LocalAuthState {
  waiting,
  authenticating,
  done,
  failed,
}

enum EncryptionState {
  waiting,
  encrypting,
  done,
  failed,
}
