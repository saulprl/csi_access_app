import "package:flutter/material.dart";

class AccessLog extends StatelessWidget {
  const AccessLog({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("xdd"));
  }
}
